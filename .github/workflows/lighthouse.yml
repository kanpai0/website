name: Lighthouse

on:
  push:
    branches: [main]

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Lighthouse CI
        uses: treosh/lighthouse-ci-action@v12
        with:
          urls: |
            https://kanpai0.co
            https://kanpai0.co/recettes/mojito/
          uploadArtifacts: true
          temporaryPublicStorage: true
